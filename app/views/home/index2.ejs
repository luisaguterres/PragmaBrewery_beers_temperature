<!DOCTYPE html>
	<html>
		<head>
			<meta charset="utf-8"/>
			<title>Beers</title>
		</head>
	<body>

		<h1>Beers' Temperature</h1>

		<table>
			
			<tr>
				<td>Beer</td>
				<td>Minimum Temperature</td>
				<td>Maximum Temperature</td>
			</tr>
			<% for(var i = 0; i < beers.length; i++){ %>
				<tr>
					<td><%= beers[i].name %></td>
					<td><%= beers[i].tempMin %></td>
					<td><%= beers[i].tempMax %></td>
				</tr>
			<% } %>

		</table>

	</body>
</html>